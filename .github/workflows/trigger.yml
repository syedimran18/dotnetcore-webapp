name: Push-to-EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to EC2 on master branch push
    runs-on: self-hosted

    steps:
      - name: Deploy to Server 1
        uses: easingthemes/ssh-deploy@main
      - name: Remove File
        run: |
         rm -f text
        working-directory: /home/ubuntu/app/app1
